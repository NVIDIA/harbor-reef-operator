apiVersion: harbor-reef.nvidia.com/v1alpha1
kind: ProxyCache
metadata:
  name: proxy-nvcr-test
spec:
  type: private
  name: proxy-nvcr-test
  url: https://nvcr.io
  credentials:
    secretName: ngc-api-secret
    usernameKey: username
    passwordKey: password
