apiVersion: harbor-reef.nvidia.com/v1alpha1
kind: ProxyCache
metadata:
  name: ($pcName)
spec:
  type: private
  name: ($pcName)
  url: https://example.com
  credentials:
    usernameKey: u
    passwordKey: p
